import 'package:flutter/material.dart';



class FootballComments extends StatelessWidget {
  const FootballComments({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('Comments')),
    );
  }
}